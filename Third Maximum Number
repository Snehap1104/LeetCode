class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        s=list(set(nums))
        s.sort(reverse=True)
        if len(s)>=3:
            return s[2]
        else:
            return max(s)    

       

class Solution:
    def countPrefixes(self, words: List[str], s: str) -> int:
        count = 0
        for i in words:
            x = len(i)
            if i == s or s[:x]==i:
                count += 1
        return count         
        
